#include <stdio.h>

int main()
{

    int a, b;
    a=5;
    b= a-2;
    if (a>b)
    {
        printf("%d est plus grand que %d\n",a,b);
    }
    else
    {
        printf("%d n'est plus grand que %d\n",a,b);
    }
    return(0);
}


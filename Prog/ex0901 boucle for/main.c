#include <stdio.h>

int main()
{
    int x;
    for(x=0; x=10; x=x+1)
    {
        puts("Ne vous l'ai-je pas encore dit ?");
        break;     // break permet de sortir de la boucle for si on ne le met pas la boucle se repette sans cesse//

    }

    return 0;
}
